@model IEnumerable<Capa_de_Datos.Models.Usuario>

<<<<<<< HEAD
@{
    Layout = "~/Views/Shared/_LayoutGeneral.cshtml";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/StyleUsuario.css">
</head>
<div class="content-container user-list">
    <h1>Lista de Usuarios</h1>

    <p>
        <a asp-action="Crear" class="btn btn-primary">Crear Nuevo Usuario</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Email</th>
                <th>Tipo</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var usuario in Model)
            {
                <tr>
                    <td>@usuario.IdUsuario</td>
                    <td>@usuario.Nombre</td>
                    <td>@usuario.Apellido</td>
                    <td>@usuario.Email</td>
                    <td>@usuario.Tipo</td>
                    <td>
                        <a asp-action="Detalles" asp-route-id="@usuario.IdUsuario">Detalles</a> |
                        <a asp-action="Editar" asp-route-id="@usuario.IdUsuario">Editar</a> |
                        <a asp-action="Eliminar" asp-route-id="@usuario.IdUsuario">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
=======
<h1>Lista de Usuarios</h1>

<p>
    <a asp-action="Crear" class="btn btn-primary">Crear Nuevo Usuario</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Email</th>
            <th>Tipo</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var usuario in Model)
        {
            <tr>
                <td>@usuario.IdUsuario</td>
                <td>@usuario.Nombre</td>
                <td>@usuario.Apellido</td>
                <td>@usuario.Email</td>
                <td>@usuario.Tipo</td>
                <td>
                    <a asp-action="Detalles" asp-route-id="@usuario.IdUsuario">Detalles</a> |
                    <a asp-action="Editar" asp-route-id="@usuario.IdUsuario">Editar</a> |
                    <a asp-action="Eliminar" asp-route-id="@usuario.IdUsuario">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
>>>>>>> ba3530e52b780c9f309df0acce60ad2366138fa7
